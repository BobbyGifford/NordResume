import styled from 'styled-components'
import { Colors } from '../../../global'

export const CSSProjects = styled.div`
    color: ${Colors.nord6};
`;

export const CSSImage = styled.img`
    max-width: 15vw;
`

export const CSSCardBody = styled.div`
    color: ${Colors.nord6};
    background-color: ${Colors.nord3}
`